"""Syndrome coding. Takes a cost vector plus the bits and says which values to change.

This layer knows nothing about file formats or crypto. It sees arrays and bit strings.
"""

# TODO(skeleton): from sieng.coder.stc import embed, extract

# TODO(skeleton): __all__ = ["embed", "extract"]
