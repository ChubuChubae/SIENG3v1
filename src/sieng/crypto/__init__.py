"""Produces bits indistinguishable from random, and proves who sent them.

This layer must be auditable on its own, so it may not import anything from sieng
except sieng.common. Enforced by import-linter.
"""
